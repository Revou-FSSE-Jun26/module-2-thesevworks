# PostgreSQL connection string used by app.py (os.getenv("DATABASE_URL"))
DATABASE_URL=postgresql://username:password@localhost:5432/revoshop_db

# Secret key for signing JWT tokens (os.getenv("JWT_SECRET_KEY"))
JWT_SECRET_KEY=change-me-to-a-random-string
